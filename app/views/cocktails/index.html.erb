<div class="list_of_all_cocktails">
  <h1>List of saved cocktails:</h1>
    <div class="row">
    <% @cocktails.each do |cocktail| %>
      <div class="card col-xs-12 col-sm-4 col-lg-3">
        <h2><%=cocktail.name%></h2>
        <%= link_to "Details", cocktail_path(cocktail) %>
      </div>
    <% end %>
  </div>
  <br>
</div>
